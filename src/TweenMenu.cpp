#include "PCH.h"
#include "tweenmenu.h"


namespace Scaleform
{
	namespace TweenMenu {

		// Start the animation
		REL::Function<voidFn> StartCloseMenu(REL::ID(51838));

		// Close the Tween Menu, This function needs to be called to allow the animation to finish;
		REL::Function<voidFn> CloseMenu(REL::ID(51839));
	}
}

